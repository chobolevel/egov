<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans 
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context 
         http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/task 
         http://www.springframework.org/schema/task/spring-task.xsd">

    <!-- @Scheduled 작동을 위한 태그 -->
    <!-- 이 설정은 내부적으로 AsyncAnnotationBeawnPostProcessor를 등록 -->
    <!-- 다른 설정 파일에 xxx:annotation-driven이 있는 경우 Spring에서는 이를 중복으로 인식하여 예외를 발생 -->
    <!-- annotation-driven 중복 등록을 제거 -->
    <!-- 
    <task:annotation-driven scheduler="defaultScheduler" />
     -->

    <!-- 스케줄러 풀 사이즈 정의 -->
    <task:scheduler id="defaultScheduler" pool-size="5" />

</beans>
